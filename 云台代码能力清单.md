# HelmswheelKing 云台代码能力清单

> 本文档仅包含云台相关模块，不含底盘、参考工程内容。供框架开发者对照新框架是否覆盖现有需求。

## 1. 项目基本信息

| 项目 | 说明 |
|------|------|
| 机器人类型 | 云台（Gimbal） |
| MCU | STM32F407IGH6 |
| RTOS | FreeRTOS |
| 通信总线 | CAN1, CAN2, UART(DMA) |
| 调试接口 | SEGGER RTT |

---

## 2. HAL 层能力清单

### 2.1 CAN 总线

**文件路径**: `HAL/CAN/`

| 能力 | 说明 |
|------|------|
| 多设备管理 | `ICanBus` 接口，通过 `CanDeviceId` 枚举管理 CAN1/CAN2/CAN3 |
| CAN 帧收发 | `ICanDevice::send(Frame)` / `ICanDevice::receive(Frame)` |
| 接收回调注册 | `ICanDevice::register_rx_callback(RxCallback)` |
| 批量回调触发 | `ICanDevice::trigger_rx_callbacks(const Frame&)` |
| 全局单例访问 | `get_can_bus_instance()` 获取总线实例 |
| 便捷方法 | `get_can1()` / `get_can2()` 快速获取设备引用 |

**Frame 结构体**:
```cpp
struct Frame {
    uint8_t data[8];
    uint32_t id;
    uint8_t dlc;
    uint32_t mailbox;
    bool is_extended_id;
    bool is_remote_frame;
};
```

**框架需要提供的**: CAN 多设备管理、帧收发、回调分发机制。

---

### 2.2 UART 总线

**文件路径**: `HAL/UART/`

| 能力 | 说明 |
|------|------|
| 多设备管理 | `IUartBus` 接口，通过 `UartDeviceId` 枚举管理 UART1/3/6 |
| 数据收发 | `IUartDevice::transmit(Data)` / `IUartDevice::receive(Data)` |
| DMA 收发 | `transmit_dma(Data)` / `receive_dma(Data)` |
| DMA 空闲中断接收 | `receive_dma_idle(Data)` — 关键能力，IMU 数据接收依赖此 |
| 接收回调注册 | `register_rx_callback(RemoteDataCallback)` |
| ORE 错误清除 | `clear_ore_error(Data)` — 清除 UART 溢出错误并重启 DMA |
| 获取 UART 句柄 | `get_handle()` 返回 `UART_HandleTypeDef*` |

**框架需要提供的**: UART DMA 空闲中断接收、ORE 错误自动恢复。

---

### 2.3 DWT 高精度定时器

**文件路径**: `HAL/DWT/DWT.hpp`

| 能力 | 说明 |
|------|------|
| 时间差计算 | `GetDeltaT(uint32_t*)` 返回 float 秒 |
| 64 位时间差 | `GetDeltaT64(uint32_t*)` 返回 double 秒 |
| 时间轴获取 | `GetTimeline_s()` / `GetTimeline_ms()` / `GetTimeline_us()` |
| 延时 | `Delay(float seconds)` |
| 单例模式 | `DWTimer::getInstance(CPU_mHz)` |

**框架需要提供的**: 高精度计时（基于 DWT CYCCNT），供 PID/ADRC 等算法计算 dt。

---

### 2.4 Logger 日志

**文件路径**: `HAL/LOGGER/logger.hpp`

| 能力 | 说明 |
|------|------|
| RTT 日志输出 | 基于 SEGGER RTT，无需串口 |
| 日志级别 | TRACE / INFO / WARNING / ERROR / FATAL |
| 彩色输出 | ANSI 颜色转义序列 |
| 时间戳 | 每条日志自动附带 `HAL_GetTick()` 毫秒时间戳 |
| 格式化输出 | 支持 printf 风格可变参数 |

**框架需要提供的**: 基于 RTT 的日志系统，支持级别过滤和彩色输出。

---

### 2.5 Assert 断言

**文件路径**: `HAL/ASSERT/asster.hpp`

| 能力 | 说明 |
|------|------|
| 运行时断言 | `assert_always(expr)` 输出文件名、行号、函数名 |

---

### 2.6 PWM 用户层

**文件路径**: `HAL/PWM/pwm_user.hpp`

> 文件当前为空，预留接口。

---

## 3. BSP 层能力清单

### 3.1 电机系统

#### 3.1.1 电机基类 MotorBase

**文件路径**: `BSP/Motor/MotorBase.hpp`

| 能力 | 说明 |
|------|------|
| 统一数据结构 | `UnitData`：角度(度/弧度)、速度(rad/s, rpm)、电流(A)、扭矩(Nm)、温度(°C) |
| 国际单位转换 | 所有电机子类将原始反馈统一转换为国际单位 |
| 在线检测 | `isConnected(id_state, id_ring)` — 超时判定 + 蜂鸣器报警 |
| 掉线查询 | `getOfflineStatus()` 返回掉线电机编号 |
| 使能状态管理 | `setIsenable()` / `getIsenable()` |
| 增量角度 | `getAddAngleDeg()` / `getAddAngleRad()` |
| 时间戳更新 | `updateTimestamp(id)` |

**模板参数**: `N` 为电机数量，编译期确定。

**框架需要提供的**: 电机统一接口（国际单位数据、在线检测、使能管理）。

---

#### 3.1.2 DJI 电机 (GM3508 / GM2006 / GM6020)

**文件路径**: `BSP/Motor/Dji/DjiMotor.hpp`

| 能力 | 说明 |
|------|------|
| CAN 数据解析 | `Parse(Frame)` — 按接收 ID 匹配电机，字节序转换 |
| 国际单位转换 | `Parameters` 结构体自动计算转换系数（减速比、力矩常数等） |
| 发送数据设置 | `setCAN(int16_t data, int id)` — 按电机 ID 填充发送缓冲区 |
| CAN 帧发送 | `sendCAN()` — 统一发送到 CAN1 |
| 增量角度计算 | 自动处理 360° 跳变 |

**云台使用的型号及参数**:

| 型号 | 减速比 | 力矩常数(Nm/A) | 反馈电流上限 | 实际电流上限(A) | 编码器分辨率 |
|------|--------|----------------|-------------|----------------|-------------|
| GM6020 | 1.0 | 0.7 | 16384 | 3 | 8192 |
| GM3508 | 1.0 | 0.3 | 16384 | 20 | 8192 |

**构造方式**:
```cpp
// 云台典型用法：2个6020（Pitch/Yaw）+ 1个3508（拨弹）
GM6020<2> motor6020(0x204, {1, 2}, 0x1FF);  // 接收0x205/0x206, 发送0x1FF
GM3508<1> motor3508(0x200, {1}, 0x200);      // 接收0x201, 发送0x200
```

**框架需要提供的**: DJI 电机 CAN 协议解析（0x200/0x1FF/0x2FF 收发 ID 体系）。

---

#### 3.1.3 LK 电机 (LK4005)

**文件路径**: `BSP/Motor/LK/Lk_motor.hpp`

| 能力 | 说明 |
|------|------|
| CAN 数据解析 | `Parse(Frame)` — 按 `init_address + recv_idxs_[i]` 匹配 |
| 位置控制 | `ctrl_Position(id, angle, speed)` — 发送 0xA4 命令 |
| 扭矩控制 | `ctrl_Torque(id, torque)` — 发送 0xA1 命令，限幅 ±2048 |
| 使能/失能 | `On(id)` 发送 0x88 / `Off(id)` 发送 0x81 |
| 清除错误 | `ClearErr(id)` 发送 0x9B |
| 多圈角度累计 | `getMultiAngle(id)` / `setAllowAccumulate(id, bool)` — 自动处理 360° 跳变 |
| 批量电流控制 | `ctrl_Multi(int16_t[4])` — 一次发送 4 个电机电流，ID=0x280 |
| 独立发送缓冲区 | 每个电机独立的 `send_data_[N][8]`，支持先 setData 再 sendCAN |

**LK4005 参数**: 减速比 10.0, 力矩常数 0.06, 编码器分辨率 65536

**框架需要提供的**: LK 电机协议（独立命令帧格式、多圈角度累计、批量控制）。

---

#### 3.1.4 DM 达妙电机 (J4310 / S2325)

**文件路径**: `BSP/Motor/DM/DmMotor.hpp`

| 能力 | 说明 |
|------|------|
| MIT 模式控制 | `ctrl_Mit(id, pos, vel, KP, KD, torq)` — 16+12+12+12+12 位编码 |
| 角度速度模式 | `ctrl_AngleVelocity(id, pos, vel)` — 浮点直传，ID=0x100+idx |
| 速度模式 | `ctrl_Velocity(id, vel)` — 浮点直传，ID=0x200+idx |
| 使能/失能/清错 | `On(id, Model)` / `Off(id, Model)` / `ClearErr(id, Model)` — 按 ID 区分模式 |
| 数据解析 | `Parse(Frame)` — 位域解析 angle(16bit)/velocity(12bit)/torque(12bit) |
| uint/float 互转 | `float_to_uint()` / `uint_to_float()` — MIT 模式压缩编解码 |

**云台使用的型号及参数**:

| 型号 | 位置范围(rad) | 速度范围(rad/s) | 扭矩范围(Nm) | KP范围 | KD范围 |
|------|--------------|----------------|-------------|--------|--------|
| J4310 | ±12.56 | ±45.0 | ±18.0 | 0~500 | 0~5.0 |
| S2325 | ±12.5 | ±50.0 | ±10.0 | 0~500 | 0~5.0 |

**注意**: DM 电机固定使用 **CAN2** 发送。

**框架需要提供的**: DM 电机 MIT/位置速度/速度三种模式协议、CAN2 支持。

---

### 3.2 IMU (HI12)

**文件路径**: `BSP/IMU/HI12Base.hpp` + `BSP/IMU/HI12_imu.hpp`

| 能力 | 说明 |
|------|------|
| 帧头搜索 | 在 82 字节缓冲区中搜索 `0x5A 0xA5` 帧头，校验长度=76 |
| CRC16 校验 | CRC-CCITT 校验，校验帧头+数据段 |
| 数据解析 | 加速度(g)、角速度(°/s)、欧拉角(°)、四元数 |
| 累计 Yaw 角 | `GetAddYaw()` — 自动处理 360° 跳变 |
| 角度映射 | `GetPitch_180()` → 0~180°, `GetYaw_360()` → 0~360° |
| 在线检测 | `isConnected()` — 超时判定 + 蜂鸣器 IMU 报警 |
| ORE 清除 | `ClearORE(huart, pData, Size)` — UART 溢出自动恢复 |

**框架需要提供的**: IMU 数据帧解析（帧头搜索+CRC校验）、累计角度计算。

---

### 3.3 遥控器 (DT7)

**文件路径**: `BSP/RemoteControl/DT7.hpp`

| 能力 | 说明 |
|------|------|
| 数据解析 | `parseData(const uint8_t*)` — 18 字节协议 |
| 通道数据 | ch0~ch3（摇杆）、scroll（滑轮）、s1/s2（开关） |
| 摇杆归一化 | `get_left_x/y()` / `get_right_x/y()` → -1.0~1.0 |
| 坐标数据 | `get_left/right_stick_x/y()` → -660~660 |
| 鼠标数据 | x/y/z 移动量、左/右键状态 |
| 键盘数据 | `get_key(Keyboard)` — 16 个按键位掩码判断 |
| 死区补偿 | `SetDeadzone(float)` + `DeadzoneCompensation(float)` |
| 在线检测 | `isConnected()` — 超时判定 + 蜂鸣器遥控器报警 |

**框架需要提供的**: DT7 遥控器 18 字节协议解析、摇杆归一化、死区补偿。

---

### 3.4 状态监控与蜂鸣器

**文件路径**: `BSP/Common/StateWatch/`

| 能力 | 说明 |
|------|------|
| 超时检测 | `StateWatch` — 设定阈值，`UpdateTime()+CheckStatus()` 判定在线/离线 |
| 蜂鸣器管理 | `BuzzerManagerSimple` 单例 — 队列式响铃请求 |
| 电机报警 | `requestMotorRing(id)` — 按 ID 号响铃对应次数 |
| 遥控器报警 | `requestRemoteRing()` — 长音 |
| IMU 报警 | `requestIMURing()` — 超长音 |
| 板间通信报警 | `requestCommunicationRing()` — 两声长音 |
| PWM 控制 | 蜂鸣器通过 PWM 驱动，值=238 |

**框架需要提供的**: 统一的设备在线检测机制、蜂鸣器报警管理（队列+PWM）。

---

### 3.5 按键 (SimpleKey)

**文件路径**: `BSP/SimpleKey/SimpleKey.hpp`

| 能力 | 说明 |
|------|------|
| 边沿检测 | 上升沿 `getRisingEdge()` / 下降沿 `getFallingEdge()` |
| 长按检测 | `getLongPress()` — 阈值 500ms |
| 点击检测 | `getClick()` — 短按释放判定 |
| 翻转状态 | `getToggleState()` — 每次按下翻转 |
| 通用输入 | `update(uint16_t keyValue)` — 不限于布尔量 |

**框架需要提供的**: 按键边沿/长按/点击检测。

---

### 3.6 有限状态机

#### 云台 FSM

**文件路径**: `BSP/Common/FiniteStateMachine/FiniteStateMachine_gimbal.hpp`

| 状态 | 说明 |
|------|------|
| STOP | 停止状态 |
| VISION | 视觉模式 |
| MANUAL | 手动模式 |
| KEYBOARD | 键盘控制模式 |

| 能力 | 说明 |
|------|------|
| 状态切换 | `SetState(left, right, online)` — 根据左右开关组合切换 |
| 状态查询 | `Get_Now_State()` / `Get_Now_State_Name()` |
| 时间统计 | `Get_State_Run_Time(state)` / `Get_State_Enter_Count(state)` |
| 统计重置 | `Reset_State_Statistics(state)` |

#### 发射 FSM

**文件路径**: `BSP/Common/FiniteStateMachine/FiniteStateMachine_launch.hpp`

| 状态 | 说明 |
|------|------|
| LAUNCH_STOP | 停止 |
| LAUNCH_CEASEFIRE | 停火 |
| LAUNCH_RAPIDFIRE | 连发 |
| LAUNCH_SINGALSHOT | 单发 |
| LAUNCH_KEYBOARD | 键盘控制 |

接口与云台 FSM 一致。

**框架需要提供的**: 通用 FSM 框架（状态定义、切换、时间统计、进入计数）。

---

## 4. Alg 层能力清单

### 4.1 PID 控制器

**文件路径**: `Alg/PID/pid.hpp`

| 能力 | 说明 |
|------|------|
| 位置式 PID | `UpDate(target, feedback)` → output |
| 积分限幅 | `setIntegralLimit(float)` — 防积分饱和 |
| 积分分离 | `setIntegralSeparation(float)` — 误差大时停止积分 |
| 输出限幅 | `setMax(float)` — 对称限幅 ±max |
| 参数在线调整 | `setK(kp, ki, kd)` |
| 重置 | `reset()` — 清零积分和误差 |

**框架需要提供的**: 位置式 PID，支持积分限幅/积分分离/输出限幅。

---

### 4.2 ADRC 自抗扰控制

**文件路径**: `Alg/ADRC/AdrcBase.hpp` + `Alg/ADRC/adrc.hpp`

| 能力 | 说明 |
|------|------|
| 一阶 LADRC | `FirstLADRC` — LESO(2阶) + LSEF(1阶)，参数: wc, w0, b0, h |
| 二阶 LADRC | `SecondLADRC` — LESO(3阶) + LSEF(2阶) + TD，参数: wc, w0, b0, h, r |
| 输出限幅 | `SetMinMax(min, max)` |
| 状态观测 | `GetZ1()/GetZ2()/GetZ3()` — 扰动观测值 |
| 状态重置 | `Reset()` |

**框架需要提供的**: 一阶/二阶线性 ADRC，含 ESO + NLSEF + TD。

---

### 4.3 滤波器

**文件路径**: `Alg/Filter/Filter.hpp`

| 滤波器 | 说明 |
|--------|------|
| KalmanFilter | 卡尔曼滤波，参数 Q/R 可调 |
| TDFilter | 跟踪微分器滤波，参数 R/H 可调 |
| LPFFilter | 一阶低通滤波，参数 Ratio 可调 |
| LMFFilter | 限幅滤波，参数 Limit_Ratio 可调 |

**框架需要提供的**: 常用滤波器集合（卡尔曼、低通、限幅、TD）。

---

### 4.4 前馈补偿

**文件路径**: `Alg/Feedforward/Feedforward.hpp`

| 能力 | 说明 |
|------|------|
| 上坡前馈 | `Uphill_FeedForward(slope)` — 根据坡度计算各轮前馈力 |
| 三次多项式拟合 | `calculate_gain()` — 增益系数由 MATLAB 生成 |
| 力到扭矩转换 | `Omni_ForceToTorque()` / `Mecanum_ForceToTorque()` / `steering_ForceToTorque()` |

> 云台场景可能不直接使用此模块，但框架应预留前馈接口。

---

### 4.5 斜坡规划

**文件路径**: `Alg/UtilityFunction/SlopePlanning.hpp`

| 能力 | 说明 |
|------|------|
| 平滑过渡 | `TIM_Calculate_PeriodElapsedCallback(target, feedback)` |
| 独立斜率设置 | 上升斜率 `SetIncreaseValue()` / 下降斜率 `SetDecreaseValue()` |
| 起始点设置 | `SetNowReal()` — 从当前实际值开始规划 |

**框架需要提供的**: 斜坡规划器（独立上升/下降斜率）。

---

### 4.6 通用 FSM 算法

**文件路径**: `Alg/FSM/alg_fsm.hpp`

| 能力 | 说明 |
|------|------|
| 状态管理 | 最多 10 个状态，`Set_Status()` 切换 |
| 状态阶段 | `ENABLE` / `DISABLE` 标记 |
| 时间计数 | `Count_Time` — 每个状态独立计时 |
| 定时回调 | `TIM_Calculate_PeriodElapsedCallback()` |

---

## 5. 关键依赖关系

```
云台应用层
  ├── Gimbal_FSM ──→ DT7 (开关状态)
  ├── Launch_FSM ──→ DT7 (开关状态)
  ├── PID/ADRC ──→ Motor (6020/3508) ──→ CAN HAL
  ├── IMU (HI12) ──→ UART HAL (DMA空闲中断)
  ├── LK电机 ──→ CAN1
  ├── DM电机 ──→ CAN2
  └── 所有BSP ──→ StateWatch ──→ DWT ──→ 蜂鸣器
```

## 6. 框架待办对照表

以下是框架**必须提供**的能力汇总，请逐条确认：

| # | 能力 | 依赖层 | 当前实现位置 |
|---|------|--------|-------------|
| 1 | CAN 多设备管理 + 回调分发 | HAL | `HAL/CAN/` |
| 2 | CAN1/CAN2 独立管理 | HAL | `ICanBus::get_can1()/get_can2()` |
| 3 | UART DMA 空闲中断接收 | HAL | `IUartDevice::receive_dma_idle()` |
| 4 | UART ORE 错误自动恢复 | HAL | `IUartDevice::clear_ore_error()` |
| 5 | DWT 高精度计时（CYCCNT） | HAL | `HAL/DWT/` |
| 6 | SEGGER RTT 日志（彩色+级别+时间戳） | HAL | `HAL/LOGGER/` |
| 7 | 电机统一接口（国际单位数据+在线检测） | BSP | `MotorBase<N>` |
| 8 | DJI 电机协议（3508/2006/6020） | BSP | `BSP/Motor/Dji/` |
| 9 | LK 电机协议（位置/扭矩/使能/多圈角度） | BSP | `BSP/Motor/LK/` |
| 10 | DM 电机协议（MIT/角度速度/速度三种模式） | BSP | `BSP/Motor/DM/` |
| 11 | IMU 帧头搜索 + CRC16 校验 + 数据解析 | BSP | `BSP/IMU/` |
| 12 | IMU 累计角度（360°跳变处理） | BSP | `HI12_float::GetAddYaw()` |
| 13 | DT7 遥控器 18 字节协议解析 | BSP | `BSP/RemoteControl/` |
| 14 | 摇杆归一化 + 死区补偿 | BSP | `DT7::SetDeadzone()` |
| 15 | 设备在线检测（超时判定） | BSP | `StateWatch` |
| 16 | 蜂鸣器报警管理（队列+PWM） | BSP | `BuzzerManagerSimple` |
| 17 | 按键边沿/长按/点击检测 | BSP | `SimpleKey` |
| 18 | 云台 FSM（停止/视觉/手动/键盘） | BSP | `Gimbal_FSM` |
| 19 | 发射 FSM（停止/停火/连发/单发/键盘） | BSP | `Launch_FSM` |
| 20 | 位置式 PID（积分限幅+积分分离+输出限幅） | Alg | `ALG::PID` |
| 21 | 一阶/二阶线性 ADRC | Alg | `ALG::ADRC` |
| 22 | 滤波器集合（卡尔曼/低通/限幅/TD） | Alg | `Alg/Filter/` |
| 23 | 斜坡规划器（独立上升/下降斜率） | Alg | `SlopePlanning` |
| 24 | 通用 FSM 算法（状态+计时+切换） | Alg | `Class_FSM` |
